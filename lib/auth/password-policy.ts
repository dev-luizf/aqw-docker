// The bundled registration SWF accepts passwords with five or more characters.
// Keep the identity provider and the Flash signup endpoint on the same policy.
export const MIN_PASSWORD_LENGTH = 5;
export const MAX_PASSWORD_LENGTH = 128;
